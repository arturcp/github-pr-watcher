<div
  class="hidden fixed inset-0 bg-black/50 opacity-0 transition-opacity duration-300 ease-out flex items-center justify-center"
  data-modal-target="modal"
  data-action="click->modal#close"
>
  <div
    class="bg-white w-1/3 rounded-lg shadow-lg p-6 relative"
    data-action="click->modal#stopClose"
  >
    <h2 class="text-2xl font-bold mb-4">Modal Title</h2>
    <p class="mb-4">This is a modal content area.</p>
    <button
      class="bg-white border text-slate-900 px-4 py-2 rounded hover:bg-slate-700 hover:text-white"
      data-action="click->modal#close">
      Close
    </button>
    <button
      class="bg-green-700 text-white px-4 py-2 rounded hover:bg-slate-700 hover:text-white"
      data-action="click->local-storage#store">
      Save
    </button>
  </div>

</div>
